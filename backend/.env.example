# Copy this file to .env and replace the development-only values.
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=accessible_games
DB_PASS=replace-with-a-local-password
DB_NAME=accessible_games
JWT_SECRET=replace-with-a-long-random-secret
PORT=5000

# Optional local Ollama-compatible voice interpretation service.
VOICE_LLM_MODEL=
VOICE_LLM_HOST=

